services:
  postgres:
    container_name: postgres
    image: postgres:18
    environment:
      POSTGRES_DB: "carStoreDb"
      POSTGRES_USER: "postgres"
      POSTGRES_PASSWORD: "123"
    volumes:
      - postgres-data:/var/lib/postgresql
    ports:
      - "5432:5432"

volumes:
  postgres-data: